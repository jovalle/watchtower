# Watchtower Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# Plex Server Configuration
# ============================================

# Your Plex server URL (e.g., http://192.168.1.100:32400)
# Find this in Plex Settings > Remote Access, or use your server's IP
PLEX_SERVER_URL=http://localhost:32400

# Your Plex authentication token
# To find your token:
#   1. Open Plex Web and sign in
#   2. Browse to any media item
#   3. Click "Get Info" > "View XML"
#   4. Look for "X-Plex-Token" in the URL
# Or visit: https://support.plex.tv/articles/204059436-finding-an-authentication-token-x-plex-token/
PLEX_TOKEN=your-plex-token-here

# Unique identifier for this Watchtower instance
# Used in Plex API headers - change if running multiple instances
PLEX_CLIENT_ID=watchtower-001

# ============================================
# Application Settings
# ============================================

# Session secret for cookie signing (generate a secure random string)
# Generate with: openssl rand -base64 32
SESSION_SECRET=generate-a-secure-secret-here

# Port for the web server (default: 9001)
PORT=9001

# ============================================
# Optional: External Services
# ============================================

# TMDB API key for movie/TV recommendations and logos
# Get a free API key at: https://developer.themoviedb.org/
# Leave empty to disable recommendations and logo features
TMDB_API_KEY=

# Data directory for caching (logos, metadata)
# Defaults to ./data in development, /data in production/Docker
# DATA_PATH=./data

# ============================================
# Optional: Trakt Integration
# ============================================

# Trakt API client ID for watchlist sync
# 1. Go to: https://trakt.tv/oauth/applications
# 2. Create a new application (name: Watchtower, redirect: urn:ietf:wg:oauth:2.0:oob)
# 3. Copy the "Client ID" (NOT the Client Secret)
# BOTH client ID and username are required for Trakt integration
TRAKT_CLIENT_ID=

# Trakt username for public watchlist sync
# Your public Trakt watchlist will be merged with Plex watchlist
# Make sure your Trakt profile and watchlist are set to public
TRAKT_USERNAME=

# ============================================
# Optional: IMDB Integration
# ============================================

# IMDB user watchlist IDs (comma-separated)
# Find your user ID in your IMDB profile URL: imdb.com/user/ur12345678
# Supports user watchlists (ur...) and custom lists (ls...)
# Example: IMDB_WATCHLISTS=ur12345678,ls012345678
# Note: Watchlists must be set to public on IMDB
IMDB_WATCHLISTS=

# ============================================
# Docker Deployment Notes
# ============================================
#
# Required environment variables for Docker:
#   - SESSION_SECRET: Required for session management
#   - PLEX_SERVER_URL: Required for Plex connectivity
#   - PLEX_TOKEN: Required for Plex authentication
#   - PLEX_CLIENT_ID: Optional (defaults to watchtower-001)
#   - PORT: Optional (defaults to 9001)
#   - TMDB_API_KEY: Optional (recommendations/logos disabled if not set)
#   - DATA_PATH: Optional (defaults to /data for persistent cache storage)
#
# Mount a volume for persistent logo cache:
#   docker run -v watchtower_data:/data ...
